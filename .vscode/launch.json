{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node-terminal",
      "name": "Run Script: find-new-rules",
      "request": "launch",
      "command": "yarn run find-new-rules",
      "cwd": "${workspaceFolder}"
    }
  ]
}